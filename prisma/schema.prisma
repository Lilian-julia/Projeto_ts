// This is your Prisma schema file,// learn more about it in the docs: https://pris.ly/d/prisma-schema// Tabela Usuariomodel Usuario {  id       Int      @id @default(autoincrement())  nome     String  senha    String  telefone String  endereco String  }// Tabela Produtosmodel Produto {  id         Int      @id @default(autoincrement())  nome       String  categoria  String  valor      Float  quantidade Int  descricao  String}generator client {  provider = "prisma-client-js"}datasource db {  provider = "postgresql"  url      = env("DATABASE_URL")}
